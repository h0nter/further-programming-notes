### Example solution to CW2
```java
boolean unique;
int i = 0;
while (unique && x < grid.length - 1){
  while (unique && i < grid.length - 1){
    int j = 0;
    while(unique && j < grid.length -1){
      if (i != j){
        Check if numbers are same
      }
    }
  }
}
```
